[project]
name = "fastapi-auth"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "alembic>1.14,<1.15",
    "argon2-cffi>=25.1.0",
    "fastapi[all]>0.115,<0.116",
]

[dependency-groups]
dev = [
    "ipython>=9.14.1",
]
